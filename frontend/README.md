# Frontend (Next.js)
## Scripts
- `npm run dev` — desenvolvimento
- `npm run build` — build de produção
- `npm start` — iniciar produção

Configure `NEXT_PUBLIC_API_URL` no Netlify apontando para o backend (ex: https://seu-backend.com).
